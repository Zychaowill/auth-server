spring:
  datasource:
     driverClassName: com.mysql.jdbc.Driver
     url: jdbc:mysql://127.0.0.1:3306/authserver?characterEncoding=utf8
     username: root
     password: root
  thymeleaf:
      cache: false
      mode: HTML5
      content-type: text/html
  jpa:
    hibernate:
          ddl-auto: update
    show-sql: true
logging:
  level:
    org:
      springframework:
             security: INFO